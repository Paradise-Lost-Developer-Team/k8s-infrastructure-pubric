apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: funya-mura-mods-pvc-new
  namespace: mc-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem
